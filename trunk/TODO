TODO - aKu - New Generation

QUICKEXTRACT: 
  2) raggruppare i due checkbox in "quickextract"
  3) aggiungere bottone e funzione new directory in "quickextract"
  4) abilitare delete archive in "quickextract"
  5) add dir with password
  6) preserve path
  9) non ci sono i path di estrazione preferiti

MAIN WINDOW:
  1) ordinamento logico per tutti i file della main
  2) ordinamento su colonne packed, size, data, ratio
  3) controllare il numero max di destination dirs
  4) icona lampeggiante con timer durante operazioni
  5) aggiungere menu rapidi collapse all e expand all

ZIP:
  1) convertire modified in QDate
 
RAR:
  1) archivi rar corrotti, inserire icona apposita
  2) funzione repair archive
  3) in archivi con errori all'interno, aku si blocca, mentre da riga di comando continua
  4) nell'add dir disabilitare l'aggiunta dir con passoword per i file headerprotected;
  5) passare tutte le operazioni attraverso la temp directory (-wtempdir)
  7) creazione archivi splittati
  8) apertura archivi splittati

TAR:
  1) convertire modified in QDate
  2) forse è possibile implementare il rename

GENERALE:
  2) conversioni tra i formati; rar -> zip; rar -> tar e così via
  4) dopo che si sono aggiunte file e directory agli archivi, al ricarimento dell'archivio, evidenziare con un 
     colore i nuovi file
  5) il dock comment compare sempre troppo lontano, in particolare in alto a sinistra all'avvio

OVERWRITE DIALOG:
  1) Solito but QLIST quando si fanno più rename
  2) Nome del file in estrazione e nome del file da sovrascrivere non coincidono
  3) bug rename, cliccando sugli altri campi diventano editabile -> grave
  4) non far sparire il processing dialog quando appare l'overwrite

PROCESSING DIALOG:
  1) nell'add file, add dir, la dimensione delle cose da aggiungere è errata
  2) disabilitare la ratio bar sottostante nell'add files, add dir

RATIOBAR:
  PROBABILMENTE RISOLTO  1) BUG GRAVE -> crash random a causa del setRatio in rar.cpp (funzione di repaint)

TIP:
  1) la barra non funziona sempre a dovere... a volte rimane bloccata a metà, a volte rimane piccolissima
     (si nota soprattutto agli add dir)

NEW ARCHIVE:
  1) selezione multipla dei file;
  2) abilitare split archive rar;

EXTRACTION DIALOG:
  1) rivedere graficamente
  2) attivare salvataggio, caricamento e reset delle opzioni e suddividerle a seconda del tipo di compressore

RARPROCESS:
 